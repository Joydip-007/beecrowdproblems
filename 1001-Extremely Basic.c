#include <stdio.h>
 
int main() {
 
    int A, B;
    scanf("%d %d", &A, &B);
    
    printf("X = %d\n", A+B);
 
    return 0;
}



//Don't forget to remove the space in the file name. Otherwise, the code won't run!
